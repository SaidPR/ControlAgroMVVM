export default class ProductionRecord {
    constructor(id, name, date, boxes, buckets) {
        this.id = id;
        this.name = name;
        this.date = date;
        this.boxes = boxes;
        this.buckets = buckets;
    }
}
